# Default: real backend via same-origin /api. Never put secrets here.
VITE_USE_MOCKS=false
VITE_API_BASE_URL=/api
# Development proxy only; not included in the browser bundle.
VITE_PROXY_TARGET=http://127.0.0.1:8000
